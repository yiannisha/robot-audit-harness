# Limitations

Reports describe observed behavior, not payload meaning. TLS 1.3 and encrypted client hello can reduce metadata. DNS attribution is time-aware evidence, not proof that a hostname owns an IP. The virtual `packets.pcap` is a small evidence marker; authoritative packet capture requires the Linux backend and tcpdump.

